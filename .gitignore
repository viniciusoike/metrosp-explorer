.Rproj.user
.Rhistory
.RData
.Ruserdata
*.Rproj
rsconnect/
.DS_Store

# manifest.json is intentionally tracked: Connect Cloud reads it for app metadata.
# Package installation is handled by renv.lock (renv/.gitignore excludes the library).
